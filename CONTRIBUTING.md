# Contributing

Use a focused branch and pull request, even for solo maintenance. Keep commits reviewable, run the repository validation command, and attach the resulting evidence. Human approval is not required; required automated checks must pass.