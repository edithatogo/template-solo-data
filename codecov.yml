coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: 90%
comment:
  layout: "reach,diff,flags,tree"
  behavior: default
github_checks:
  annotations: true